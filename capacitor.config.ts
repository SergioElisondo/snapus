import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.snapus.snapus',
  appName: 'snapus',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
